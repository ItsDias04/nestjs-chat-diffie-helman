# Example environment variables for Chat Diffie-Hellman
# Copy this file to `.env` and update values before running the services.

# General
NODE_ENV=development
PORT=3000

# JWT / Auth
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=3600s

# Database (Postgres example)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=chat_db

# TypeORM / connection URL alternative
# DATABASE_URL=postgres://user:pass@localhost:5432/chat_db

# CORS (comma-separated origins for the API)
CORS_ORIGINS=http://localhost:4200

# Optional: WebSocket host/port if different from API
WS_HOST=localhost
WS_PORT=3000

# Any other service-specific env vars can be added per subproject
